package scala.meta
package internal
package ui

private[meta] object inferTokens {
  def apply(tree: Tree, proto: Option[Tree]): Tokens = {
    // TODO: implement this
    Tokens.Synthetic(Nil: _*)
  }
}
