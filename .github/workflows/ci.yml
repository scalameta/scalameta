name: CI
on:
  push:
    branches:
      - main
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Every Scala version lives here — bump in ONE place and all jobs follow.
# Upgrade:
#  - replace the previous oldest (scalaX_Y) with the previous latest (scalaX)
#  - replace the previous latest (scalaX) with the new version
#  - don't need to reorder scalaX_Y, they are not designed to be monotonic
env:
  scala33: '3.3.8'
  scala38: '3.8.4'
  # 2.13
  scala213: '2.13.18'
  scala213_1: '2.13.17'
  scala213_2: '2.13.16'
  scala213_3: '2.13.15'
  # 2.12
  scala212: '2.12.21'
  scala212_1: '2.12.20'
  scala212_2: '2.12.19'
  scala212_3: '2.12.18'

# Pre-merge gate (pr-* jobs): only the most critical checks, packed into a
# few equal-length workers so it finishes about as fast as the work allows.
# No event guard, so they run post-merge too, where the post-* jobs
# (if: push) add everything else — one definition, no drift.

jobs:
  post-jvm-other-jdks:
    if: ${{ github.event_name == 'push' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        java: [ '25' ]
    steps:
      - &checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - &jdk
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java || '17' }}
          distribution: 'temurin'
          cache: 'sbt'
      - &sbt
        uses: sbt/setup-sbt@v1
      - &testjvm212
        if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala212 }}!; tests2_12/testFull'
      - &testsem212
        if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala212 }}!; testsSemanticdb/testFull'
      - &testjvm213
        if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala213 }}!; tests/testFull'
      - &testsem213
        if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala213 }}!; testsSemanticdb/testFull'
      - &testjvm33
        if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala33 }}!; tests3/testFull'
      - &testjvm38
        if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala38 }}!; tests3/testFull'

  # ===== PR gate: 5 balanced JDK-17 workers, run on PR *and* push (~8 min) =====
  pr-mima: # MiMa binary-compatibility (the single heaviest atom)
    runs-on: ubuntu-latest
    steps:
      - *checkout
      - *jdk
      - *sbt
      - if: ${{ !cancelled() }}
        run: sbt mima

  pr-jvm212-sem212-js38: # latest 2.12 parser+semanticdb + latest-3 JS
    runs-on: ubuntu-latest
    steps:
      - *checkout
      - *jdk
      - *sbt
      - *testjvm212
      - *testsem212
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala38 }}!; testsJS3/testFull'

  pr-jvm213-js33-other: # latest 2.13 parser + 3.3 JS + scala-library download
    runs-on: ubuntu-latest
    steps:
      - *checkout
      - *jdk
      - *sbt
      - *testjvm213
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala33 }}!; testsJS3/testFull'
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala213 }}; download-scala-library'
  
  pr-jvm33-sem213-other: # 3.3 parser + 2.13 semanticdb + community + scalafmt
    runs-on: ubuntu-latest
    steps:
      - *checkout
      - *jdk
      - *sbt
      - *testjvm33
      - *testsem213
      - if: ${{ !cancelled() }}
        run: sbt communitytest/testFull
      - if: ${{ !cancelled() }}
        run: ./bin/scalafmt --test

  pr-jvm38-js2: # latest-3 parser + JS on both Scala 2 lines
    runs-on: ubuntu-latest
    steps:
      - *checkout
      - *jdk
      - *sbt
      - *testjvm38
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala212 }}!; testsJS2_12/testFull'
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala213 }}!; testsJS/testFull'

  # ===== post-merge superset: deferred axes, added only on push to main =====
  post-native: # flaky Native leg — deferred off the PR gate, retried once
    if: ${{ github.event_name == 'push' }}
    runs-on: ubuntu-latest
    # Scala Native's trap-based GC yieldpoints intermittently time out on CI.
    env:
      SCALANATIVE_GC_TRAP_BASED_YIELDPOINTS: '0'
    steps:
      - *checkout
      - *jdk
      - *sbt
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala212 }}!; testsNative2_12/testFull' || sbt '++${{ env.scala212 }}!; testsNative2_12/testFull'
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala213 }}!; testsNative/testFull' || sbt '++${{ env.scala213 }}!; testsNative/testFull'
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala33 }}!; testsNative3/testFull' || sbt '++${{ env.scala33 }}!; testsNative3/testFull'
      - if: ${{ !cancelled() }}
        run: sbt '++${{ env.scala38 }}!; testsNative3/testFull' || sbt '++${{ env.scala38 }}!; testsNative3/testFull'

  post-older-scala: # older patches — one leg per slot index (2.12_N + 2.13_N)
    if: ${{ github.event_name == 'push' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        idx: [ '1', '2', '3' ]
    steps:
      - *checkout
      - *jdk
      - *sbt
      - if: ${{ !cancelled() }}
        run: "sbt '++${{ env[format('scala212_{0}', matrix.idx)] }}!; tests2_12/testFull'"
      - if: ${{ !cancelled() }}
        run: "sbt '++${{ env[format('scala212_{0}', matrix.idx)] }}!; testsSemanticdb/testFull'"
      - if: ${{ !cancelled() }}
        run: "sbt '++${{ env[format('scala213_{0}', matrix.idx)] }}!; tests/testFull'"
      - if: ${{ !cancelled() }}
        run: "sbt '++${{ env[format('scala213_{0}', matrix.idx)] }}!; testsSemanticdb/testFull'"

  post-windows: # Windows parser/JS/semanticdb (line-ending sensitivity)
    if: ${{ github.event_name == 'push' }}
    name: Windows tests
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: olafurpg/setup-scala@v14
      - run: sbtx tests/testFull
        shell: bash
      - run: sbtx testsJS/testFull
        shell: bash
      - run: sbtx testsSemanticdb/testFull
        shell: bash
